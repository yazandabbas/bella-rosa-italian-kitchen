---
import SiteLayout from "../layouts/SiteLayout.astro";
import { galleryImages } from "../data/site";
---

<SiteLayout
  title="Gallery | Bella Rosa Italian Kitchen"
  description="A gallery of Bella Rosa interiors, dishes, and chef shots."
>
  <main class="content-band page-frame">
    <div class="section-heading">
      <p class="eyebrow">Gallery</p>
      <h1>Gallery</h1>
      <p class="hero-copy">
        A quick look at the dining room, pasta, private dining setup, and kitchen.
      </p>
    </div>
    <div class="gallery-grid">
      {galleryImages.map((image) => (
        <figure>
          <img src={image.src} alt={image.alt} loading="lazy" />
          <figcaption>{image.alt}</figcaption>
        </figure>
      ))}
    </div>
  </main>
</SiteLayout>
